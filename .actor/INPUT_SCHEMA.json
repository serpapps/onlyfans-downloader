{
  "title": "OnlyFans Downloader input",
  "description": "This actor currently exposes a listing-first browser-extension surface. Runtime scraping input is not finalized yet, so the public input schema stays intentionally minimal until the real run contract is defined.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {}
}
